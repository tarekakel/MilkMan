import { FilterOnForPipe } from './filter-on-for.pipe';

describe('FilterOnForPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterOnForPipe();
    expect(pipe).toBeTruthy();
  });
});
